<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballStats">

        <!-- Κύρια οθόνη -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- R1: Επιλογή αγώνα -->
        <activity
            android:name=".activities.SelectMatchActivity"
            android:label="@string/title_select_match" />

        <!-- R1: Κάρτα αγώνα (ενδεκάδες + αλλαγές) -->
        <activity
            android:name=".activities.MatchCardActivity"
            android:label="@string/title_match_card" />

        <!-- R2: Διαχείριση στατιστικών αγώνα -->
        <activity
            android:name=".activities.ManageMatchActivity"
            android:label="@string/title_manage_match" />

    </application>

</manifest>
